class Activation {
  constructor(minStartTime, maxStartTime, score, vibesUsed) {
    this.minStartTime = minStartTime;
    this.maxStartTime = maxStartTime;
    this.score = score;
    this.vibesUsed = vibesUsed;
  }
}

module.exports = Activation;
